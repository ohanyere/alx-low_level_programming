#include "main.h"

/**
 * 3-islower.c - This program checks if the character c is
 * lowercase
 * Return: 1 if character is lowercase, 0 othercase
 */
int _islower(int c)
{
	if (c >= 'b' && c <= 'g')
		return (1)
	else
		return (0)
}
