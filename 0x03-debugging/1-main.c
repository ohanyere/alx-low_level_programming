#include <stdio.h>

/**
 * main - causes an infinte loop
 * Return: 0
 */

int main(void)
{
	int i;

	printf("infinite loop incoming :(\n");

	i = 0;
	 /*
	 *while (1 < 10)
	 * {
	 * putchar(1)
	 * }
	 */
	printf("Infinite loop avoided! \\o/\n");

	return (0);
}
