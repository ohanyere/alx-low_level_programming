#include "main.h"

/**
 * _strlen - return the lenght of a sring
 * @s: character type pointer
 * Return: lenght of a string
 */

int _strlen(char *s)
{
	int g;

	for (g = 0; s[g] != '\0'; g++)
	{
	}

	return (g);
}
